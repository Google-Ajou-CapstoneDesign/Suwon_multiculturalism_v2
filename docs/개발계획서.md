# 프론트엔드 (Flutter)
P0 — 뼈대
하단 탭 4개 구조(백과사전 / AI가이드 / 근무기록 / 설정) + 각 탭별 Navigator
온보딩(언어 선택 → 비자 유형 입력, 최초 1회)
Firebase Auth 연동 로그인/회원가입 화면
공용 위젯: Card, Chip, StepIndicator(단계 표시), DisclaimerBanner(고지문 배너)

P0 — 모듈 1 (2개 카테고리만 완성)
카테고리 목록 화면(20개 아이콘, 나머지 18개는 "준비 중" 스켈레톤 카드만)
카테고리 상세 화면: ①~⑤ 5단 카드 템플릿(필수 앱/ARC 발급용)
근로계약서 체크리스트 화면(11개 항목 체크박스, 회원가입 없이 진입 가능)

P0 — 모듈 2 (챗봇)
채팅 화면(메시지 리스트 + 입력창)
응답 카드 3종 렌더링: 사실 답변(FactAnswer) / 위험 안내(RiskNotice+라우팅 버튼) / 하단 고정 추천기관 카드
백엔드 API 연동(질문 전송 → 응답 렌더링)만 하고, 로직 자체는 백엔드에 위임

P0 — 모듈 3 (근무기록장 + 임금체불만 먼저)
데일리 훅 입력 화면: 출퇴근 시각 피커, 업무내용 칩 선택, 사진 첨부, 특이사항 체크박스
임금체불 대응 플로우 5단계 화면(유형분류 → 절차안내 → 사실입력+증빙첨부 → 서식매핑 시각화 → 기관라우팅)
DocumentMappingRow 위젯(auto/verbatim/blocked 3가지 상태 표시)

P1
산재 대응 플로우 5단계(임금체불과 동일 패턴 재사용)
사후 모니터링(근무일지 vs 원계약서 자동 대조)
설정 탭(언어/비자정보/알림)

P2
나머지 18개 카테고리 완성
비자 만료 D-30/D-7 알림 캘린더
커뮤니티/FAQ

# 백엔드 (GCP 에이전트)
P0
Firebase Auth 검증 미들웨어 (Flutter → API 요청 인증)
챗봇 에이전트 최소 구현: 사용자 메시지 수신 → 비자군(E-9/H-2/D-2)·생애주기 분류 질문 → 검수된 정적 콘텐츠 매칭 → 응답 반환 (LLM은 분류/의도파악에만 사용, 절차 설명은 정적 텍스트 그대로 반환 — 새 문장 생성 금지)
임금체불 규칙 엔진: 1단계 입력값(선택형·날짜·숫자) → 근로기준법 제36조 기준 if-then 분류 → 안내문 선택 (LLM 미사용)
표준 서식 자동 채움 API: 사실형 필드만 템플릿 치환(고용노동부 진정서 다국어 서식), 주장형 필드는 공란 반환
파일 업로드 API(Firebase Storage 연동, 원본 암호화 저장, OCR/분석 없음)
기관 라우팅 API: 사용자 위치 + 사안 유형 기준으로 고정된 기관 리스트 매칭 (외부 지도 API 연동은 최소한으로)

P1
산재 신청서(요양급여신청서) 자동 채움 API (임금체불과 동일 구조 재사용)
컨텍스트 캐싱 적용(Gemini 3.1 Pro + 반복 시스템 프롬프트 캐시) — 비용 최적화
근무기록 vs 계약서 자동 대조 로직

P2
비자 만료 알림 푸시(Cloud Scheduler + FCM)
Gemma 압축 단계 도입(비용 절감, 트래픽 늘어난 이후)
다국어 확장(중국어·베트남어 외 언어)

# DB (Firestore 설계)
P0 — 컬렉션
users: 이름, 비자유형, 비자만료일, 언어설정, 회원정보(진정서 자동채움용 사실형 데이터)
worklogs: 근무일자, 출퇴근시각, 업무내용, 사진URL, 특이사항(정상/연장/사고)
contract_checklist: 사용자별 11개 항목 체크 상태 (비회원은 로컬 임시 저장 후 로그인 시 마이그레이션)
wage_cases: 임금체불 케이스별 사실 입력값(근무시작일/종료일/미지급액/약정월급/계약서유무 등) + 진행 상태
content_categories: 모듈1 20개 카테고리 정적 콘텐츠(검수 완료본만 게시, 팀이 직접 CMS처럼 관리)
orgs: 라우팅용 기관 정보(명칭, 주소, 관할지역, 연락처)

P1
accident_cases: 산재 케이스 사실 입력값 + 진행 상태
chat_sessions: 대화 이력(에이전트 이력 저장용)

P2
faq: 챗봇 응답 소스로 활용될 FAQ 아카이브
감사로그/버전관리(6개월 주기 콘텐츠 재검수 이력)